AGENT_TOKEN=replace-with-your-token
# Optional tray server-output logging (default: off)
# Set to true to write uvicorn stdout/stderr to %APPDATA%\IntegrateAgent\logs\server_output.log
TRAY_SERVER_OUTPUT_LOG_ENABLED=false
# When logging is enabled, truncate server_output.log once per day before writing (default: true)
TRAY_SERVER_OUTPUT_LOG_DAILY_CLEAR=true
